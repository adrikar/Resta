<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rest">
        <activity android:name=".InfoUser"></activity>
        <activity android:name=".ShowPedido"/>
        <activity android:name=".RestMenu" />
        <activity android:name=".Admin.AddProd" />
        <activity android:name=".Admin.ViewImageActivity" />
        <activity android:name=".Admin.MenuAdmin" />
        <activity android:name=".Admin.LoginAdmin" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".Register" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>